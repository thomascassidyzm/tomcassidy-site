---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProgramWheel from '@/kit/ProgramWheel.svelte';
import { agileResults as program } from '@/lib/programs/agile-results';
import { assertValidProgram } from '@/kit';

assertValidProgram(program);

// JD Meier's Agile Results as a 13×4 — the kit's first SINGLE-domain wheel:
// one continuous ring of twelve practices, the Review Cycle at week 13.
export const prerender = true;
---

<BaseLayout title="Agile Results — JD Meier & Tom Cassidy" currentPage="programmes">
  <article class="program">
    <header class="prog-head">
      <p class="eyebrow">Reason-Ability · a programme by {program.byline}</p>
      <h1 class="prog-title">{program.title}</h1>
      <p class="prog-lede">{program.blurb}</p>
    </header>

    <ProgramWheel program={program} client:visible />

    <section class="prose">
      <p>
        <strong>Agile Results</strong> is JD Meier's system for getting stuff
        done — built on the Rule of 3, the Monday-Vision / Daily-Wins /
        Friday-Reflection rhythm, and wins at every scale. Here it runs as a
        13×4: twelve practices taken <em>one a week</em>, straight round the
        ring, then the <strong>{program.hub.algorithmName}</strong> — the
        system that improves the system — before the next lap. Four laps a
        year. Tap a slice to meet its practice; press <strong>Play</strong> to
        walk the thirteen.
      </p>
    </section>

    {program.domains.map((domain) => (
      <section class="domain" style={`--pig: var(--${domain.pigment})`}>
        <div class="domain-head">
          <h2 class="domain-name">{domain.name}</h2>
          <p class="domain-q">{domain.question}</p>
        </div>
        {domain.description && <p class="domain-desc">{domain.description}</p>}
        <ol class="focuses">
          {domain.focuses.map((f) => (
            <li class="focus">
              <span class="focus-wk">Week {f.week}</span>
              <div class="focus-body">
                <h3 class="focus-name">{f.name}</h3>
                <p class="focus-wisdom">{f.coach.wisdom}</p>
              </div>
            </li>
          ))}
        </ol>
      </section>
    ))}

    <footer class="prog-foot">
      <p>
        The programme is JD's; the wheel is the house engine reading one data
        file — its first single-ring shape. The words above are working drafts
        of JD's system, awaiting the authors' polish.
      </p>
    </footer>
  </article>
</BaseLayout>

<style>
  .program { max-width: 820px; margin: 0 auto; padding: 3rem 1.5rem 6rem; }

  .prog-head { margin-bottom: 1rem; }
  .eyebrow {
    font-family: var(--font-mono); font-size: 0.68rem; text-transform: uppercase;
    letter-spacing: 0.12em; color: var(--text-muted);
  }
  .prog-title {
    font-family: var(--font-display); font-optical-sizing: auto;
    font-size: clamp(2.6rem, 7vw, 4rem); font-weight: 420; letter-spacing: -0.02em;
    color: var(--text-primary); margin: 0.4rem 0 0.6rem; line-height: 1;
  }
  .prog-lede {
    font-family: var(--font-body); font-style: italic;
    font-size: clamp(1.2rem, 2.4vw, 1.5rem); line-height: 1.45;
    color: var(--text-secondary); max-width: 44ch;
  }

  .prose {
    font-family: var(--font-body); font-size: 1.12rem; line-height: 1.7;
    color: var(--text-secondary); max-width: 62ch; margin: 1rem auto;
  }
  .prose strong { color: var(--text-primary); font-weight: 600; }

  .domain { margin-top: 3.5rem; padding-top: 1.5rem; border-top: 1px solid var(--hairline); }
  .domain-head { display: flex; align-items: baseline; gap: 0.9rem; flex-wrap: wrap; margin-bottom: 1rem; }
  .domain-name {
    font-family: var(--font-display); font-optical-sizing: auto;
    font-size: clamp(1.6rem, 4vw, 2.2rem); font-weight: 460; letter-spacing: 0.02em;
    color: var(--pig);
  }
  .domain-q {
    font-family: var(--font-mono); font-size: 0.72rem; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--text-muted);
  }
  .domain-desc {
    font-family: var(--font-body); font-size: 1.05rem; line-height: 1.6;
    color: var(--text-secondary); max-width: 62ch; margin-bottom: 1.4rem;
  }

  .focuses { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; }
  .focus { display: grid; grid-template-columns: 5.5rem 1fr; gap: 0.5rem 1rem; align-items: start; }
  @media (max-width: 520px) { .focus { grid-template-columns: 1fr; gap: 0.2rem; } }
  .focus-wk {
    font-family: var(--font-mono); font-size: 0.72rem; letter-spacing: 0.05em;
    padding-top: 0.35rem; white-space: nowrap; color: var(--pig);
  }
  .focus-name {
    font-family: var(--font-display); font-optical-sizing: auto;
    font-size: 1.3rem; font-weight: 480; color: var(--text-primary); line-height: 1.15;
  }
  .focus-wisdom {
    font-family: var(--font-body); font-size: 1.04rem; line-height: 1.6;
    color: var(--text-secondary); margin-top: 0.3rem;
  }

  .prog-foot {
    margin-top: 4rem; padding-top: 1.5rem; border-top: 1px solid var(--hairline);
    font-family: var(--font-body); font-style: italic; font-size: 1.02rem;
    line-height: 1.6; color: var(--text-muted); max-width: 62ch;
  }
</style>
